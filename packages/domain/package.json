{
  "name": "@dredmorpedia/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "benchmark:search:official": "vitest run benchmark/search-performance.test.ts --reporter=verbose --maxWorkers=1 --no-file-parallelism",
    "lint": "eslint src test benchmark --max-warnings=0",
    "test": "vitest run --dir test",
    "typecheck": "tsc -p tsconfig.json"
  }
}
